# Automatically sent via Segment Page tracking
name: Page Viewed
description: User has viewed a page
rules:
  '$schema': http://json-schema.org/draft-04/schema#
  type: object
  properties:
    context:
      $ref: ../meta/context.yaml
    properties:
      $ref: ../meta/context.yaml
