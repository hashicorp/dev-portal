---
componentName: InlineLink
---

The `InlineLink` component should be used with a body of text. See more documentation [the HDS Product Figma](https://www.figma.com/file/noyY6dUMDYjmySpHcMjhkN/?node-id=2365%3A21590).

Internally, this component uses two other components:

- `MaybeInternalLink`: to automatically determine whether or not `next/link` should be used
- `Text`: to ensure conformance font size and weight with the design system

## Props

| Prop name    | Description                                                                      | Required? | Default value | Type                |
| ------------ | -------------------------------------------------------------------------------- | --------- | ------------- | ------------------- |
| `className`  | A string of one or more class names. Applied last to the rendered `<a>` element. | No        | undefined     | string              |
| `href`       | The destination of the link.                                                     | Yes       | N/A           | string              |
| `children`   | The content to render within the `<a>` element.                                  | Yes       | N/A           | ReactNode           |
| `textSize`   | The `size` passed to the inner `Text` component.                                 | No        | 300           | TextProps[`size`]   |
| `textWeight` | The `weight` passed to the inner `Text` component.                               | No        | regular       | TextProps[`weight`] |

## Playground

<LiveComponent>{`
<InlineLink href="/" textWeight="medium">link to home</InlineLink>
`}</LiveComponent>

## Examples

### Inline with other text

<LiveComponent>{`
<p>This is some text with a link <InlineLink href="/">link to home</InlineLink>. Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p>
`}</LiveComponent>

### Different `textSize` values

<LiveComponent>{`
<>
  <InlineLink href="/" textSize={100}>link with size=100</InlineLink>
  <br />
  <InlineLink href="/" textSize={200}>link with size=200</InlineLink>
  <br />
  <InlineLink href="/" textSize={300}>link with size=300</InlineLink>
</>
`}</LiveComponent>

### Different `textWeight` values

<LiveComponent>{`
<>
  <InlineLink href="/" textWeight="regular">link with textWeight=regular</InlineLink>
  <br />
  <InlineLink href="/" textWeight="medium">link with textWeight=medium</InlineLink>
  <br />
  <InlineLink href="/" textWeight="semibold">link with textWeight=semibold</InlineLink>
  <br />
  <InlineLink href="/" textWeight="bold">link with textWeight=bold</InlineLink>
</>
`}</LiveComponent>
