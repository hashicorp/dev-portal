---
componentName: CheckboxField
---

## Only label

<CheckboxField label="This is the label text" />

## Label + error

<CheckboxField errors={['This is the error']} label="This is the label text" />

## Label + helper text

<CheckboxField helperText="This is the helper text" label="This is the label" />

## Label + helper text + error

<CheckboxField
	errors={['This is the error']}
	helperText="This is the helper text"
	label="This is the label"
/>

## Label + helper text + error

<CheckboxField
	errors={['First error message', 'Second error message']}
	helperText="This is the helper text"
	label="This is the label"
/>
